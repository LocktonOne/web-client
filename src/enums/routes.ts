export enum RoutePaths {
  Root = '/',
  Dashboard = '/dashboard',
  Login = '/login',
  AdminLogin = '/admin-login',
  Roles = '/roles',
  Administrators = '/administrators',
  Users = '/users',
  KycRequests = '/kyc-requests',
}
